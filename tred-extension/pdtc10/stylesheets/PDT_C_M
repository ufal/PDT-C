context: PDT_C_M

node: #{customform}<? $${#name} eq "s" ? '${id}' : ($${form} ? '${form}' : '${#content/form}') ?>
node: #{custommlemma}<? $${lemma} ? '${lemma}' : '${#content/lemma}' ?>
node: #{customtag}<? $${tag} ? '${tag}' : '${#content/tag}' ?>

style: #{Line-coords:n,n,n,n}
